image: python: 3.5

pages:
  stage: deploy
  script:
  - pip install mkdocs
  - pip install mkdocs-material
  - mkdocs build -d public
  artifacts:
    paths:
    - public
  only:
  - master
